Full modular programming language.
Change the lexer, parser, and semantic rules as you want.
Tokens are defined as constructive types; AST nodes are functions attached as a branch for the returning type; semantics is controlled as any other compile-time code executed.

There are only two eternal truths here:
- The current implementation is a mess
- The mess should be easier to modify
